const cikart = (a, b) => {
  return a - b;
};

function topla(a, b) {
  return a + b;
}

const text = "text";

const user = {
  name: "Ogeday",
  surname: "Yalcin",
  age: 20,
};

export { topla, cikart, text, user };
